#include "main.h"

/**
 * _strlen- calculates the lenght of the string
 * Return: lenght of string
 * @s: string
 */
int _strlen(char *s)
{
	int le;

	s = "string";
	le = _strlen(s);

	return (le);
}
